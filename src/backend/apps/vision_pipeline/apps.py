from django.apps import AppConfig


class VisionPipelineConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "vision_pipeline"
    verbose_name = "Pipeline de Visão Computacional"
